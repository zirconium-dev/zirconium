#!/usr/bin/env bash

set -xeuo pipefail

mkdir -p "${SRCDIR}/cache"

if [ ! -e "${SRCDIR}/cache/flathub.flatpakrepo" ] ; then
	curl --retry 3 --retry-delay 2 --retry-max-time 60 -fsSLo "${SRCDIR}/cache/flathub.flatpakrepo" "https://dl.flathub.org/repo/flathub.flatpakrepo"
fi
